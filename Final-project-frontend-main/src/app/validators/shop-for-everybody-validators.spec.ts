import { Shop4EverybodyValidators } from './shop-for-everybody-validators';

describe('Shop4EverybodyValidators', () => {
  it('should create an instance', () => {
    expect(new Shop4EverybodyValidators()).toBeTruthy();
  });
});
